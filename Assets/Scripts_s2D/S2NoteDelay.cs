using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class S2NoteDelay : MonoBehaviour
{
    GameObject note;

    // Start is called before the first frame update
    void Start()
    {
        // 비활성화 혹은 스피드 0인 상태로 시작
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
